@model IEnumerable<myapp.Models.Funny>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VeryGood)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mouse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Headphones)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VeryGood)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mouse)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Headphones)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
